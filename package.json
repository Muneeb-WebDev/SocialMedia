{
  "name": "social-media-app",
  "version": "1.0.0",
  "description": "A fully functional social media web app built with HTML, Tailwind CSS, and Vanilla JavaScript",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./src/output.css --minify",
    "build:copy": "rimraf dist && cpx \"src/pages/*\" dist && cpx \"src/javascript/**\" dist/javascript && cpx \"src/output.css\" dist && cpx \"img/**\" dist/img && cpx \"src/_redirects\" dist",
    "build": "npm run build:css && npm run build:copy",
    "watch:css": "tailwindcss -i ./src/input.css -o ./src/output.css --watch",
    "preview": "npx serve dist"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.17"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "cpx": "^1.5.0",
    "postcss": "^8.5.6",
    "rimraf": "^6.1.2",
    "tailwindcss": "^4.1.17"
  }
}
